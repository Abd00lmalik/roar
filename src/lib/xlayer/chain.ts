/**
 * X Layer chain definitions — backward-compatibility re-export.
 *
 * The canonical definitions live in chains.ts.
 * This file exists so existing imports of @/lib/xlayer/chain keep working.
 */
export { xLayerTestnet, xLayerMainnet } from "./chains";
